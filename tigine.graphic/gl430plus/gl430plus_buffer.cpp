#include "gl430plus_buffer.h"

namespace tigine {
	gl430plus_buffer::gl430plus_buffer(const gl430plus_buffer_descriptor& desc, const void* data) {
		if ((desc.flags & VERTEX_BUFFER) > 0) {

		}
	}

	
}